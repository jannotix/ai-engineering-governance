# Project State

`/ai-init` creates project-local `.ai/` state.

The directory should normally be committed because it records architecture and verification state that applies to that source revision.

Secrets are prohibited.

The plugin remains the authoritative reusable governance source. Project `.ai/` files contain only decisions, status, evidence, and project-specific configuration.
